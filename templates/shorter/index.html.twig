{% extends 'base.html.twig' %}

{% block body %}
<form method="post">
    <input name="link" type="text" required>
    <button>make</button>
</form>
<br/>
    {% if shortUrl %}
        {{ shortUrl }}
    {% endif %}
{% endblock %}
